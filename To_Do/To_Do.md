Artifactory Clean up

Every Monday...scale down crashloop backoff pods
Disable cron jobs which are agressive
IAH-East
what quota is set on the other clusters
move stage workloads - work with zia

kubectl get cm rook-ceph-mon-endpoints -o yaml

Instead of bs why bx is showing up
